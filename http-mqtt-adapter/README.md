# htt-mqtt-adapter

Receives data from external sources that are not using MQTT, such as an HTTP source, and publish them to the specified MQTT broker.

It is an optional piece which only ensures that the rest of the system can work uniformly