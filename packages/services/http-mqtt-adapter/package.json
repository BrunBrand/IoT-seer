{
  "name": "@iot-seer/http-mqtt-adapter",
  "private": "true",
  "dependencies": {
    "mqtt": "^5.13.2",
    "zod": "^4.0.5",
    "@iot-seer/config": "1.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp config.toml dist/",
    "start": "node dist/index.js"
  }
}
