{
  "name": "@iot-seer/anomaly-detection-service",
  "private": "true",
  "dependencies": {
    "@iot-seer/config": "1.0.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && cp config.toml dist/",
    "start": "node dist/index.js"
  }
}
